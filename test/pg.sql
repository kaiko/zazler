CREATE TABLE alfa(id integer not null, a text, b text);
INSERT INTO alfa VALUES (1, 'a', 'b');

CREATE TABLE users(id integer not null, login text, passwd text);
INSERT INTO users(id, login, passwd) VALUES (1, 'jack', 'alfa'), (2, 'peter', 'beta'), (3, 'nick', 'eeta');

CREATE TABLE w(a text, b text, c text);

